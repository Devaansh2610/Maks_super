__pycache__/
*.pyc
venv/
.env
secrets/
models/
mac_companion/venv/
mac_companion/.env
mac_companion/*.log
# langgraph dev's local runtime cache (checkpoints/store pickles) -- not
# source, machine-specific, regenerated on every run.
.langgraph_api/
# Durable conversation memory (AsyncSqliteSaver, maks/graph/supervisor.py) --
# runtime data, not source.
maks_memory.sqlite*
