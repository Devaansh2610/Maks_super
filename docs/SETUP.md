# Setup guide — moved

The full step-by-step setup guide (Ubuntu install, Google OAuth, Notion
token, Mac companion, running it, LangSmith, and testing MCP tools with MCP
Inspector) now lives in the main **[README.md](../README.md)**, in the "Full
setup guide" section, so it stays in one place instead of two documents
drifting apart.
